#include <stdio.h>
#include <math.h>

float trigo (int, int);

int main()
{
    float ans = trigo(3,4);
    printf("%.2f", ans);

    return 0;
}

float trigo (int x, int y) {
    
    float n;
    
    n=sqrt((x*x)+(y*y));
    
    return n;
}
